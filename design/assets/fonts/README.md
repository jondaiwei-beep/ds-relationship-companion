# Bundled fonts

- `Cormorant Garamond`: display and ritual typography.
- `Inter`: UI and functional typography.

Font files and their OFL licenses are committed to the project so local developer font installation is unnecessary. If the app code is later moved under a nested directory, update asset paths while preserving the same binaries and family names.

