# Domain contracts

This directory holds shared state models and invariants for pairing, roles, consent, agency, privacy, tasks, rituals, acknowledgements, notifications, and subscription.

These contracts are cross-screen rules. In particular, role never grants consent; pause/leave rights must remain available according to the approved product contract; private and shared data boundaries must be explicit.
