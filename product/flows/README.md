# Product flows

Flow files connect requirements across screens. They define entry conditions, state transitions, system and human actions, recovery paths, exits, and platform differences.

Use stable IDs such as `FLOW-PAIRING-001`. A flow may reference several requirement IDs and Screen IDs, but must not rely on a screen image to define behavior.
